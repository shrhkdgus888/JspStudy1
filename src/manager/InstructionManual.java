package manager;
//manager Package 클래스 기능 설명
public class InstructionManual {
	
	//1. DB에 저장될 테이블을 정의하여 OracleDB에 추가한다.

	//2. 정의된 OracleDB 테이블에 맞춰서 MemberVo(또는 DTO=Data Transfer Object)를 테이블과 맵핑할 수 있도록 정의한다.

	//3. OracleDB 테이블에 CRUD 할 수 있는 쿼리문이 있을것이고, 그 쿼리문을 전송할 수 있게끔 DAO를 만든다.

	//4. 실직적인 기능(비즈니스 로직)을 사용자에게 제공하기 위해 MemberService 클래스를 만들어 서비스 한다.

	//   * JdbcConnectionUtil 클래스는 커넥션을 편하게 사용하려고 만든것일 뿐, 따로 없어도 상관이 없고 나중에 
	//	   DBCP(DataBase Connection Pool)을 만들어서 해결 가능하다. 

}

